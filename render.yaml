services:
  - type: web
    name: eduvy-ai-backend
    env: docker
    rootDir: backend
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
