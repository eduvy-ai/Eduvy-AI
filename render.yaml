services:
  - type: web
    name: eduvy-ai-backend
    env: python
    rootDir: backend
    buildCommand: bash build.sh
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    nativePackages:
      - ffmpeg
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
